const express = require('express');
const app = express();
const port = 3000;

app.get('/charge', (req, res) => {
    res.send('Ini link charge!');
});

app.listen(port, () => {
    console.log(`Server jalan di http://localhost:${port}`);
});
